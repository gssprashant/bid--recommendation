apiVersion: v1
kind: Secret
metadata:
  name: gss-bid-secrets
type: Opaque
data:
  # Example: echo -n "your_fred_api_key" | base64
  # Replace with your base64-encoded secret value or create it with kubectl create secret
  FRED_API_KEY: "REPLACE_BASE64_FRED_KEY"
